# 输出与证据契约

## ACadSharp便携候选契约

`acadsharp-portable-evidence/0.1`是独立的原始候选契约，不属于V5/V6/V7/V10/V13/V18
正式输出。至少必须保留：源文件名和SHA-256、后端及版本、DWG版本、总状态、解析通知、
遍历问题、未支持实体计数，以及每条候选的实体类型、Handle、根空间、块路径、图层和
实例键。坐标字段只对该解析器当前结果负责。

`ATTDEF`必须作为`definition_template_not_placed_value=true`的定义模板单列，不能伪装成
已放置TEXT或ATTRIB。MINSERT可以记录行列数和间距，但在逐项展开前不能把单个INSERT候选
当作全部阵列实例。

以下门禁固定适用：

- `formal_backend_equivalent=false`；
- `absence_proven=false`；
- `original_dwg_opened_by_parser=false`且`analysis_copy_only=true`；
- ATTRIB坐标为`parser_value_not_backend_equivalent`；
- 布局视口可见性和嵌套块有效图层为`not_implemented_unverified`；
- 任一解析通知、遍历错误、未支持可达实体或非均匀缩放存在时，状态为
  `portable_readonly_candidate_unresolved`。

候选输出不得直接喂给V16/V19/V21数量与定位闭合逻辑。阴性检索只表示当前候选API范围内
未命中，不能证明图中不存在目标。

字段对比输出`acadsharp-zwcad-field-comparison/0.1`固定保持
`candidate_field_comparison_unresolved`、`formal_backend_equivalent=false`和
`absence_proven=false`。只有语义相同的字段才能计入匹配；诊断性的嵌套组合变换、旋转圆
外包框或ATTDEF兼容口径不得被解释为正式后端等价。

## 单图后端路由契约

`cad-backend-route/0.1`必须保留固定顺序`ACadSharp → ZWCAD → AutoCAD2023`、每次尝试的
状态/原因/独立输出、所选后端、原图SHA-256前后值及`source_unchanged`。以下门禁固定：

- `absence_proven=false`；
- 原图哈希变化时必须为`source_hash_changed_safe_stop`，且不得选中后端；
- 选中中望或AutoCAD 2023前，必须确认执行成功、分析副本哈希未变、六类JSON齐全；
- 不得静默合并ACadSharp与原生输出；
- 三者均失败时保留`manual_review_required_no_backend`，不写成“图中没有”。

AutoCAD/中望字段对照`native-cad-backend-comparison/0.1`中，字体/实体外包框、纸空间
整体视口相机值和运行时视口编号是诊断项。它们不覆盖Handle、文字、WCS、方向、
图层或模型展示视口核心字段，也不将`backend_equivalent`或`absence_proven`提升为true。

## V18 顶层输出

`<WorkRoot>\output\V18目录内容复筛.md`、`.csv`、`.json` 必须至少包含：

- V17原分类和原因；
- V18内容决策及命中文字、块、图层或实体证据；
- 内容指纹路径和导出状态；
- `absence_proven=false`；
- 若执行完整流程，对应V16最终状态。

`promoted_primary` 只表示文件升级进入完整证据流程，不表示数量成立。
`content_negative` 只表示本次API可读范围未命中，不证明图中没有阻尼器。
`content_unresolved` 必须给出失败、代理对象或跳过对象原因。

## V20 无人值守执行输出

`V20中望导出执行.csv` 必须逐图记录输入路径、`success/failed/timeout`、耗时、
已完成命令、本轮精确终止的ZWCAD PID和字体策略路径。

`font-policy/font-policy.json` 必须记录替代字体绝对路径与SHA-256、任务FMP、
运行前的FONTALT/FONTMAP/FILEDIA/CMDDIA、恢复账本路径、是否应用上次残留
恢复，以及“替代字体不代表原图视觉效果”的边界。字体文件缺失、任务未完成或
用户配置未恢复时，不得将该图标记为成功。

## V19 跨 DWG 输出

`<WorkRoot>\v19\V19跨DWG证据组.md`、`.csv`、`.json` 必须至少包含：

- 楼栋集合和楼层范围；
- 唯一主视图、辅助来源及各自视图角色；
- 主轴网是否充分；
- V12输入清单、执行状态、物理模板数和未映射实例数；
- 安全停止原因。

`V19未分组证据.csv` 必须保留楼栋/楼层未解析或上游文件缺失证据。文件名明确
楼栋时优先采用该范围；文件名无楼栋时才回退到块内/行内文字。不同DWG单图数量
不得相加，V19安全停止不得写成“图中无阻尼器”。

## V21 阻尼器定位输出

`V21阻尼器定位总表.csv` 每条设备必须包含：总表唯一ID、组ID、局部设备ID、
楼栋、楼层、数字轴间与比例、字母轴间与比例、主来源、主图框、主实例键、
主视图世界坐标、楼层证据和定位状态。

同时输出 `V21阻尼器定位未决.csv`、`.json`、`.md`。只有以下条件全部满足时，
总状态才允许为 `device_location_registry_complete`：

- 所有计数组均闭合，且没有未分组或载入问题；
- 每条记录的楼栋、楼层、轴间、主来源、实例键和世界坐标均非空；
- 总表设备ID无重复；
- 单栋楼层范围可明确展开；多栋共用平面已有逐栋楼层调和证据；
- 主实例没有无证据地同时落入多套楼栋轴网。

V22 可处理证据闭合的不规则轴网：旋转/扇形直轴须由同轴号两个远端位置确定；
弧轴须轴号—圆弧唯一关联、至少两弧共圆心且各有切向直线延伸。设备须唯一落入
直轴条带/扇区或弧轴有效角域/切线范围。最外轴外侧不超过相邻轴距25%的近邻可
写成 `最后轴>OUT@比例`，不得虚构下一轴。任何条件不满足时必须进入未决，不得
按最近轴号硬配。

## V23 几何定位抽查输出

`V23逐台几何定位证据.csv/json`必须保留每台设备的楼栋、楼层、轴间、世界坐标、
主实例键、定位方法、轴号文字句柄、几何实体句柄、匹配距离、局部SVG路径和
证据状态。`V23人工抽查索引.html`应离线可打开。

- 相同模板跨多个楼层只生成一张SVG，逐台CSV仍逐层保留记录；
- 正交轴至少保留边界轴号文字句柄；
- 旋转/扇形直轴还须保留最近平行轴线实体句柄；
- 弧轴须保留圆弧、切向延伸、轴号文字句柄和匹配距离；
- 原轴间/比例与重算不一致或证据缺失时使用`evidence_trace_unresolved`；
- SVG是API坐标证据图，不是DWG打印效果，不得提升V19/V21状态。

## V24 风险抽样与中望回查输出

- `V24风险分层全量.csv`保留全部模板的P0～P3、分值、风险原因和抽样状态。
- `V24抽查任务.csv`与`V24AI轻量抽查清单.json`只包含强制复核和分层抽中项。
- P0/P1必须全部复核；P2/P3必须按楼栋确定性抽样，不得只挑容易查看的模板。
- `V24未定位证据组.csv`单列没有逐台坐标的上游证据组，SVG不能替代该缺口。
- `V24中望回查定位.lsp`只能只读打开非同步目录中的DWG分析副本；原图、
  OneDrive文件、缺失副本或当前图名不一致时必须停止。
- LISP只允许缩放和临时选择集高亮，不保存、不修改DWG。

## V16 顶层输出

`<WorkRoot>\output\V16运行汇总.md`、`.csv`、`.json` 必须至少包含：

- 原始文件名和专业路由状态；
- 图框数、阻尼器候选数、图面计入数、调和数量候选；
- V13 阻塞错误数；
- 最终证据状态；
- 结论边界。

`input_manifest.csv` 必须保留原路径、副本路径、SHA-256、路由状态和原因。
目录输入还必须保留专业、图纸角色、主流程分类、优先级、规则证据、规范化文件族和文件名内八位日期。

## 每图分析证据

至少保留：

- 阻尼器实例候选 CSV；
- 阻尼器自动计数 Markdown；
- 楼层数量、型号数量、方向和多栋共用调和 CSV；
- 图框候选及文字归属 CSV（若识别成功）；
- V14 布局视口可见性 Markdown/CSV；
- 六类中望 API 原始 JSON。

## 数量字段

数量输出应能回查：

- 楼栋、楼层或连续楼层范围；
- 设备型号、X/Y/方向不明；
- 唯一实例键、根实例、父实例、块定义、世界坐标/范围；
- 图框/视图角色、布局、视口和重复展示；
- 带文字证据、种子几何证据、数量表值、差异；
- 已确认、候选、未解析、冲突或安全停止状态。

## 禁止自动下结论

- `design_quantity_candidate_reconciled` 不等于合同、供货或生产数量。
- 参数表、检测抽样、说明示例和图例实例不得当作图面设备补数。
- 合成图结果不得作为真实项目证据。
- 不从邻近文字拼接缺失型号、楼层、数量、材料或规范条款。
- 不隐去未归属、未解析、读取错误或跨视图冲突。
- 不把 `supporting`、`reference`、`older_revision`、`exact_duplicate` 或 `uncertain` 写成“图中无阻尼器”；它们只是未进入本轮主计数。
- 不把 `promoted_primary` 写成新增设备数量，也不把多个升级图的单图数量直接相加。
- 不绕过V19/V12，把结构平面、梁、柱、墙、板图中的重复表达分别累加。
- 不因只有主视图就跳过V21定位；也不把多栋共用图无证据地乘到所有楼栋。
- 文件名日期筛选不能代替正式图纸版本、发图记录或设计变更证据。

## 最小人工复核说明

当状态不是数量候选闭合时，报告必须明确：

1. 已经由 API 确认了什么；
2. 哪一层证据未闭合；
3. 需要查看哪张图、哪个图框/布局/坐标或哪个对象句柄；
4. 为什么不能自动给出数量。
