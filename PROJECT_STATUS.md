# 项目状态

## 当前状态

- 当前版本及稳定Release：`12.5.0` / `v12.5.0`。
- 状态日期：2026-08-17。
- 本仓库维护四项通用能力：中望CAD只读提取、文字索引、轴网定位、阻尼器数量识别。
- 原始DWG只读；运行时先复制到非同步工作目录并校验SHA-256，关闭CAD时不保存。
- 中望导出器仅分发源码，必须针对目标电脑的中望CAD版本现场编译。
- V18/V16正式数量管线可自动发现本机中望；单图Agent路由已实现
  `ACadSharp → ZWCAD → AutoCAD2023 → AutoCAD2020 → AutoCAD2018 → AutoCAD2014`。
- AutoCAD采用与中望共享的导出器源码，但现场引用当前宿主Autodesk程序集单独编译；
  不分发DLL。只支持64位2023 R24.2、2020 R23.1、2018 R22.0、2014 R19.1；
  2014仅处理AC1027及更早DWG，任何版本不兼容时均不自动转换。
- AutoCAD 2023的合成布局视口图和两套非公开真实DWG只读字段级回归已闭合可比核心字段；
  2020/2018/2014已完成源码适配和自动门禁，仍待对应真实宿主回归；
  宿主外包框、纸空间整体视口相机值和关闭视口运行时编号仅作诊断，
  仍不声明后端整体等价。
- ACadSharp 3.6.51候选读取器已在两套真实DWG、原生中望变换合成图和布局视口合成图上完成
  只读副本字段回归，可在无CAD安装时生成原始候选证据。根插入与部分基础字段可对应，但
  ATTRIB、复杂嵌套变换、带bulge多段线、MINSERT展开、视口、有效图层和动态块未闭合，
  因此不得接入正式V16/V19数量或定位状态。
- 可选多模态配置只服务高风险局部证据复核，不改变正式证据状态，也不解除D4冻结。
- 12.2新增的最小净空/生根导出器只消费既有D4状态与共享梁台账，不打开DWG、不做跨项目自动
  识别。真实项目输入与输出不进入仓库。

## D4冻结决策

D4阻尼器深化自动识别（梁号/梁高、上下梁、升降板、上翻墩、安装净空等）于
2026-08-07冻结。冻结原因不是单一代码缺陷，而是跨设计院图纸的梁语义、局部
标注和跨楼层配准仍需要大量项目特定判断；当前继续投入的时间和模型成本与可靠性
不匹配。

D4跨项目自动识别源码和既有试验记录仍保留在原开发工作区，不作为已发布能力，也不继续自动
运行。仓库中的`experimental/deepening`只保留一个已独立测试的状态汇总器，不包含自动识图管线、
真实项目状态或工程确认结果。

## 恢复条件

只有同时满足以下条件才重新开启D4研发：

1. 先定义一个固定、隐藏既有深化图的跨项目盲测集；
2. 自动确认项保持零错误，未决项无静默丢失；
3. 单项目耗时和模型消耗设定硬上限，并明显优于当前人工流程；
4. 梁、板、局部详图和楼层配准均能保留文件、图框/布局、坐标、句柄及计算证据；
5. 未达到门槛时允许安全停止，不用多模态结果或人工答案反填正式算法。

## 已知边界

- 代理对象、缺失字体、图框归属或视口可见性未闭合时必须保留未决。
- 数量结果是设计布置候选，不是合同供货数量或生产放行数量。
- SVG是坐标证据的局部表达，不是DWG原图渲染，也不用于正式视觉验收。
- 无多模态AI可读取JSON/CSV并运行本地命令；中望CAD只负责前置只读提取。
