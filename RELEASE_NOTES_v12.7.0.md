# v12.7.0

本版本在v12.5.0多版本AutoCAD辅助路由基础上，补齐新会话自举和AutoCAD D2L 2.2
正式梁文字证据后备。原始DWG只读、证据不静默合并、失败安全停止和D4跨项目自动识别
冻结边界保持不变。

## 新增与调整

- 新增Session Doctor与`SESSION_BOOTSTRAP.md`。仓库检出和已安装Codex Skill均可在新窗口
  生成`cad-toolkit-session/1.0`，落盘工具版本、入口完整性、工作目录、本机CAD宿主和
  Skill漂移状态，不依赖上一段聊天记忆。
- AutoCAD现场构建器新增D2L 2.2目标，复用同一份旁路数据库算法源码，仅在构建阶段将
  `ZwSoft.ZwCAD.*`映射为`Autodesk.AutoCAD.*`并引用本机API程序集，不混用或分发中望DLL。
- 新增`运行AutoCADD2L旁路数据库索引.ps1`：要求目标与宿主为不同分析副本，启动前检查
  64位宿主和DWG头，拒绝覆盖已有证据，运行前后核对两份DWG SHA-256，并记录独立执行清单。
- D2L正式成功门禁固定为`D2L-sidedb-2.2`、无截断、跳过对象0、全部记录包含可见/可打印
  字段、文字记录包含世界方向空间；不满足时安全停止，普通V5/V6/V7/V10/V13不能冒充D2L。
- 已安装CAD Skill与深化Skill同步了AutoCAD D2L入口和执行清单SHA校验，新窗口可按
  `ACadSharp候选 → 中望D2L → AutoCAD D2L`边界继续工作。

## 验证

- 根工具测试：`67 passed`。
- 仓库内置Codex Skill测试：`59 passed`。
- 深化模块回归：`723 passed, 8 skipped`。
- 48个PowerShell脚本在PowerShell 7与Windows PowerShell 5.1中解析错误均为0。
- AutoCAD 2023 R24.2从已安装Skill现场编译8个DLL成功；真实梁图D2L执行状态为
  `success`，跳过对象0、无截断，目标和宿主分析副本运行前后SHA-256一致。
- 与相同展开根的中望D2L对照：顶层16960、直接文字10002、属性270、块实例55一致；
  24910条直接/递归文字按原文、根句柄、块路径、WCS、世界方向、可见/可打印组合键逐条一致。
- 禁入扫描未发现DWG/DXF、项目证据、编译DLL、许可证、凭证或本机运行结果进入仓库。

## 明确边界

- AutoCAD与中望对LINE/POLYLINE存在8条净分类差异。本Release只确认正式梁文字证据兼容，
  不声明所有几何实体或宿主行为整体等价。
- AutoCAD 2020/2018/2014尚未在对应真实宿主现场编译和运行；共享源码与门禁不能替代实机验证。
- ACadSharp仍是无CAD安装时的轻量候选后端，不是正式原生宿主等价替代。
- D4跨项目梁号、梁高、净空和连接节点自动识别继续冻结；本Release不恢复自动深化能力。
- 数量、定位和梁文字结果仍是可回查工程证据，不替代专业审图、合同数量或生产放行。
