# 环境与运行边界

## 必需环境

- Windows 10/11。
- PowerShell 7 或 Windows PowerShell 5.1。
- Python 3.10+；当前 Python 脚本只使用标准库。
- 能正常打开目标 DWG 的中望 CAD，并提供 `ZwManaged.dll`、`ZwDatabaseMgd.dll` 和 COM 自动化接口。
- 中望安装目录内存在 `fonts\HZTXT.SHX` 与 `fonts\simplex.shx`；缺失任一
  文件时V20拒绝无人值守运行。
- .NET Framework 4.x 的 64 位 C# 编译器。

## 路径要求

- 原始 DWG 可位于项目目录或 OneDrive，但保持只读。
- 工作目录按以下优先级确定：显式 `-WorkRoot`；环境变量 `CAD_READING_WORK_ROOT`；默认 `%LOCALAPPDATA%\CadReadingToolkit\Work`。
- 工作目录不得是磁盘根目录，也不得位于 `OneDrive` 或系统识别的同步根目录。
- 当前项目电脑仍可显式使用 `G:\CodexWork`，但其他电脑不需要 G 盘，也不需要修改脚本源码。
- DLL、缓存、日志和可重建中间文件不得写入 OneDrive。
- 中望导出器必须针对目标电脑安装的中望版本现场重新编译，不分发预编译 DLL。
- 中望/AutoCAD安装路径不得写死。统一入口未传`-ZwcadRoot`时按
  `CAD_ZWCAD_ROOT`、卸载注册表和常见安装目录自动发现；也可先运行
  `zwcad\发现CAD安装.ps1 -Vendor Any`审查结果。
- 当前只验证中望CAD执行后端。AutoCAD安装发现仅用于兼容性预检，不能加载中望
  程序集，也不能在AutoCAD适配器和真实DWG回归完成前自动切换。

## V18 执行内容

统一入口会：

1. 由V16路由 DWG 专业并完成V17目录分层；
2. 只复制辅助/不确定图到预扫描工作目录并核对 SHA-256；
3. 编译轻量内容指纹导出器，只读扫描实际插入可达内容；
4. 生成V18升级、保留、参考、阴性和未决状态；
5. 把原主图与升级图复制到完整流程工作目录并再次核对 SHA-256；
6. 编译六个完整中望导出器；
7. 通过V20逐图隔离的中望 COM 会话执行 V5 文字、V5 图框、V6 实例、V7 方向、V10 几何、V13 可见性导出；
8. 无保存关闭图纸；
9. 生成图框、数量、布局视口和汇总证据；
10. 由V19按楼栋/楼层组织主视图与辅助视图，并在前置证据充分时调用V12；即使只有主视图也生成V21逐台定位总表，V22在存在V10.1几何时补充旋转/扇形直轴及同心弧轴定位；
11. 自动调用V23重算已定位模板，生成句柄、匹配距离、局部SVG和离线HTML。
12. V23自动调用V24风险分层抽样；AI先读轻量清单，必要时用中望LISP在只读
    工作副本中按坐标/句柄回查。

所有 `.ps1` 以 UTF-8 BOM 保存，兼容 PowerShell 7 与 Windows PowerShell 5.1。
SHA-256 由 .NET 流式计算；允许失败的图框分析以退出码记录安全停止，不中断其他图纸。

## V20 无人值守字体和进程隔离

- 运行前关闭用户正在操作的中望CAD。默认发现已有ZWCAD进程即安全停止，避免
  复用或误关用户会话。
- V20临时设置 `FONTALT=HZTXT.SHX`，并生成任务本地FMP：`sft/sft.shx`
  映射到 `simplex.shx`，宋体、黑体、仿宋、楷体及其`.shx`形式映射到
  `HZTXT.SHX`；同时临时设置 `FILEDIA=0`、`CMDDIA=0`。
- 每张DWG使用独立子进程。超时或失败只终止本轮新启动的ZWCAD，记录状态并
  继续；批次结束后恢复原字体和对话框配置。
- 配置变更前写入 `%LOCALAPPDATA%\CadReadingToolkit\v20-font-recovery.json`。
  若整批父进程被外部强杀，下一次V20在处理新图前先按账本恢复；账本损坏或
  恢复失败时安全停止。
- 字体替换只保证API流程不中断；字形、字宽、排版和特殊符号可能变化，视觉
  验收和正式出图必须补齐原字体复核。

## 代理对象和字体

- 字体替换会影响屏幕显示和视觉/OCR；API读取结果也必须保留原实体证据并抽查。
- 天正、PKPM 或其他专业代理对象可能只能得到代理实体；不可据显示正常就假定 API 已读出内部数据。
- 若导出器记录逐对象错误或关键对象缺失，应进入 `visibility_export_unresolved` 或人工核实，不得用视觉猜测补全。

## 运行失败排查

- 未发现中望：检查`CAD_ZWCAD_ROOT`，或显式传`-ZwcadRoot`；只有AutoCAD时当前
  版本应安全停止。完整边界见`CAD_BACKEND_COMPATIBILITY.md`。
- 编译失败：确认自动发现或显式传入的 `ZwcadRoot` 是当前中望安装根目录，且两个托管 API DLL 存在。
- COM 无法启动：先手动启动一次中望、完成许可/首次启动设置后关闭，再重试。
- 缺字体仍弹窗：检查 `output\font-policy\font-policy.json`、任务FMP及两个
  本机字体哈希；不要把原字体复制进分发包。
- 单图卡死：检查 `V20中望导出执行.csv` 的 `timeout`、终止PID和后续图纸
  状态；不得改成长时间复用一个CAD进程。
- 导出不完整：检查工作目录 `输出\` 中六类 JSON 是否齐全，不要直接重跑并覆盖原图。
- 内容复筛未决：检查 `prescan\output\V18内容指纹执行记录.csv`、指纹JSON的代理/跳过对象和逐图消息，不得改写成阴性。
- 图框识别失败：这是允许的安全停止；可按项目证据补充散线/块图框分支，不能降低阈值强行归框。
- V19/V21未闭合：检查 `v19\V19未分组证据.csv`、`V21阻尼器定位未决.csv` 和各组清单；缺主图、主图冲突、楼栋/楼层不明、轴网不足、设备未落入轴间或多栋楼层证据不足都应保持原状态。
- V23未闭合：检查 `v19\V23几何定位证据\V23逐台几何定位证据.json`；
  缺轴号/实体句柄、轴间重算不一致或SVG缺失时不得把模板写成证据完整。
- V24中望回查不可用：检查`safe_review_copy`是否存在、是否位于非OneDrive
  目录，并确认当前中望图名与任务图名一致。
