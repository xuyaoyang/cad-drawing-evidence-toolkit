# 安全与数据边界

## 禁止提交

- 真实DWG/DXF、PDF、图片、项目表格和运行结果；
- 中望CAD安装文件、许可证、字体文件和现场编译DLL；
- API密钥、Token、Cookie、账号、密码或个人路径配置；
- `OneDrive`、`G:\CodexWork` 或其他本机工作目录中的缓存和证据包。

## 运行原则

- 原图只读，分析从校验过SHA-256的非同步副本执行。
- 发现已有用户ZWCAD会话时，无人值守入口安全停止，不复用也不终止该会话。
- 字体替代只用于避免弹窗阻塞API导出，不代表图纸视觉效果正确。
- 导出错误、代理对象、缺字段或配置恢复失败均应保留明确未决状态。

如发现凭证或真实项目资料误入版本历史，应立即停止使用该仓库，撤销凭证并清理
Git历史，而不是仅做一次普通删除提交。
